export type Work = {
  id: number;
  authorId: number;
  authorName: string;
  title: string;
  coverImageUrl: string;
};

export type Novel = {
  id: number;
  authorId: number;
  authorName: string;
  title: string;
  description: string;
  coverImageUrl: string;
};
